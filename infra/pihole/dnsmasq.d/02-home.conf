# Custom DNS configuration for home domain
# All *.home queries resolve to orangepi5b (Traefik)
# Traefik then routes based on the hostname

# Resolve all *.home to orangepi5b IP (where Traefik is running)
address=/home/192.168.50.40