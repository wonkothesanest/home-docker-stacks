# Custom DNS configuration for homelab.local domain
# All *.homelab.local queries resolve to orangepi5b (Traefik)
# Traefik then routes based on the hostname

# Resolve all *.homelab.local to orangepi5b IP (where Traefik is running)
address=/homelab.local/192.168.50.40
