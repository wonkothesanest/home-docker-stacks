# Traefik Environment Configuration Template
# Copy this file to .env and fill in actual values

# Base domain for services (e.g., home, example.local)
DOMAIN=home

# Hostname or IP address of wonko.local for routing remote services
# This is used in dynamic.yml to route traffic to wonko.local services
# Examples: wonko.local, 192.168.1.100
WONKO_HOST=wonko.local
